import { Player } from "@/types";

// Global players are loaded from /data/global-players.json so their portraits
// always resolve to local /player-images sprite sheets.
export const players: Player[] = [];
